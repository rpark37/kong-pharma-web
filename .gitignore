.DS_Store
node_modules/
.superpowers/

# Agent skills installed by `npx skills add` — local tooling, not part of the site.
# Re-create with the same command rather than vendoring 4MB of third-party skills.
.agents/
.claude/
skills-lock.json
